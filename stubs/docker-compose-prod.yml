services:
  app:
    image: ${IMAGE_NAME:-app}:${IMAGE_TAG:-latest}
    container_name: app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    networks:
      - app_network

networks:
  app_network:
    external: true
