NODE_ENV=production
HOST=0.0.0.0
PORT=3000
NITRO_HOST=0.0.0.0
NITRO_PORT=3000

# Public runtime config (exposed to client). Prefix with NUXT_PUBLIC_
NUXT_PUBLIC_APP_URL=https://example.com
NUXT_PUBLIC_API_BASE=https://api.example.com

# Private runtime config (server only). Prefix with NUXT_
# NUXT_API_SECRET=
